<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="SmartHome.App.Views.Controls.StatCard"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Border StrokeThickness="0" Padding="16" Shadow="{StaticResource SoftShadow}"
            BackgroundColor="{Binding CardBg, Source={RelativeSource AncestorType={x:Type local:StatCard}}}">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="20"/>
        </Border.StrokeShape>

        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
            <Border HeightRequest="40" WidthRequest="40" StrokeThickness="0"
                    BackgroundColor="{Binding IconBg, Source={RelativeSource AncestorType={x:Type local:StatCard}}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="14"/>
                </Border.StrokeShape>
                <Label Text="{Binding IconText, Source={RelativeSource AncestorType={x:Type local:StatCard}}}"
                       TextColor="White" FontSize="16"
                       HorizontalOptions="Center" VerticalOptions="Center"/>
            </Border>

            <VerticalStackLayout Grid.Column="1" Spacing="2">
                <Label Text="{Binding ValueText, Source={RelativeSource AncestorType={x:Type local:StatCard}}}"
                       FontSize="22" FontAttributes="Bold"
                       TextColor="{StaticResource TextDark}"/>
                <Label Text="{Binding LabelText, Source={RelativeSource AncestorType={x:Type local:StatCard}}}"
                       FontSize="12"
                       TextColor="{Binding Accent, Source={RelativeSource AncestorType={x:Type local:StatCard}}}"/>
            </VerticalStackLayout>
        </Grid>
    </Border>
</ContentView>
